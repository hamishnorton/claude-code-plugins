{
  "name": "class-toolkit",
  "description": "Generate personalised educational resources for each student in the class",
  "version": "0.3.0",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pandoc --version || echo \"[class-toolkit] pandoc is not installed. The generate-resource skill requires pandoc to create Word documents. Install with: brew install pandoc (macOS), sudo apt-get install pandoc (Linux), or winget install JohnMacFarlane.Pandoc (Windows)\""
          }
        ]
      }
    ]
  }
}
